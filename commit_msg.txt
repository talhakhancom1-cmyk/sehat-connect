Fix profile picture fallback, add notification beep, fix OTP public flag

#1 Profile picture fallback:
- Removed lib/doctorPhotos.js which returned random Unsplash stock
  photos when no profile picture was uploaded.
- Updated DoctorAvatar.jsx to show initials on a colored circle
  when no imageUrl is provided, instead of fetching a random photo.
- Real uploaded photos still display correctly via authFileUrl().
- All 15 files using DoctorAvatar now show consistent initials fallback.
- TopBar, IncomingCallOverlay, AudioCall already used initials —
  no change needed there.

#4 OTP - public flag endpoint:
- Added GET /api/email-config/public-flags (no auth required) that
  returns only { configured, enable_signup_otp } so the Register
  page can check if OTP is enabled without admin credentials.
- Updated Register.jsx to use the public endpoint instead of the
  admin-only /email-config route.

#2 Notification beep sound:
- Created lib/notificationSound.js using Web Audio API (no external
  file needed). Plays a subtle two-tone "ding-dong" (880Hz + 1100Hz).
- Handles browser autoplay restrictions: AudioContext is created
  lazily, primed on first user interaction (click/touch/keydown).
- Added onNotificationNew() to socketClient.js to subscribe to the
  existing 'notification:new' socket event from the backend.
- TopBar now plays the beep when a real-time notification or chat
  message arrives (not on page load for old notifications).
- Only beeps for chat messages if user is NOT in the active chat
  thread (avoids double-notification when already reading).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
