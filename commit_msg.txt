Fix call regression + harden SMTP save with DOM refs

Call regression (BLOCKING):
- User table has display_name and profile_pic_url columns, NOT
  first_name/last_name/profile_image_url. The previous caller ID
  fix referenced non-existent columns, causing call:initiate to
  throw a database error that blocked all calls entirely.
- Fixed both User.findByPk queries in realtime.js to use the
  correct column names.

SMTP save fix (definitive):
- Previous formRef approach still relied on React state being
  current. Now reading critical field values (smtp_host, smtp_username,
  smtp_password, from_email) directly from DOM input refs at save
  time, completely bypassing any React state issues.
- Added type="button" to Save button to prevent implicit form
  submission behavior.
- Enhanced logging to show both DOM-read values and state values
  side-by-side for diagnosis.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
